<mah:MetroWindow x:Class="WpfMvvm02.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:local="clr-namespace:WpfMvvm02.Views"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="550" Width="1000" mah:DialogParticipation.Register="{Binding}"
        >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="55"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Menu IsMainMenu="True">
                <MenuItem Header="파일">
                    <MenuItem.Icon>
                        <iconPacks:Ionicons Kind="DocumentMD"/>
                    </MenuItem.Icon>
                    <MenuItem Header="종료" Command="{Binding AppExitCommand}">
                        <MenuItem.Icon>
                            <iconPacks:PackIconUnicons Kind="SignoutSolid"/>
                        </MenuItem.Icon>
                    </MenuItem>
                </MenuItem>
                
                <MenuItem Header="관리">
                    <MenuItem.Icon>
                        <iconPacks:RemixIcon Kind="RemoteControl2Fill"/>
                    </MenuItem.Icon>
                    
                    <!-- 하위 메뉴 아이템 -->

                    <MenuItem Header="책장르" Command="{Binding ShoDivisionCommand}">
                        <MenuItem.Icon>
                            <iconPacks:MaterialDesign Kind="Category"/>
                        </MenuItem.Icon>
                    </MenuItem>

                    <MenuItem Header="도서" Command="">
                        <MenuItem.Icon>
                            <iconPacks:PhosphorIcons Kind="BooksFill"/>
                        </MenuItem.Icon>
                    </MenuItem>


                    <MenuItem Header="회원" Command="">
                        <MenuItem.Icon>
                            <iconPacks:PhosphorIcons Kind="UserListBold"/>
                        </MenuItem.Icon>
                    </MenuItem>

                    <MenuItem Header="대여" Command="">
                        <MenuItem.Icon>
                            <iconPacks:FontAwesome Kind="TruckDropletSolid"/>
                        </MenuItem.Icon>
                    </MenuItem>

                    
                </MenuItem>
                <MenuItem Header="도움말" Command="" >
                    <MenuItem.Icon>
                        <iconPacks:PhosphorIcons Kind="QuestionMarkFill"/>
                    </MenuItem.Icon>


                    <MenuItem Header="프로그램 정보" Command="" HorizontalAlignment="Right">
                        <MenuItem.Icon>
                            <iconPacks:PackIconUnicons Kind="SignoutSolid" />
                        </MenuItem.Icon>
                    </MenuItem>
                </MenuItem>
            </Menu>
        </StackPanel>
        
        <!-- 메인 영역(장르관리, 도서관리, 회원관리, 대여관리) -->
        <ContentControl Grid.Row="1" Content="{Binding ContentCurrentView}"></ContentControl>
        
        <!--상태바-->
        <StatusBar Grid.Row="2">
            <StatusBarItem Content="Text"></StatusBarItem>
            <StatusBarItem Content="Text" HorizontalAlignment="Right"></StatusBarItem>
        </StatusBar>
    </Grid>
</mah:MetroWindow>
